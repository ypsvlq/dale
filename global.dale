build(c)
	bdir ?= build
	objdir ?= $bdir/$(_task)_obj
	outdir ?= $bdir

	LIBS += ${map lib l $libpfx$l}
	CFLAGS += ${map inc i $incpfx$i}
	CFLAGS += ${map def d $defpfx$d}

	src = ${map src s ${glob $s}}
	obj = ${map src s $objdir/${stripext $s}$objext}
	out = $outdir/$_task$($(_type)ext)

	@mkdirs obj
	@mkdirs out

	@do Compile obj src
	@do Link out obj
